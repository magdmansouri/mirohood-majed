# برنامه ارتقای کیفیت — ماجد منصوری

## ارزیابی فنی فعلی

- **Stack:** PHP server-rendered، JavaScript بدون فریم‌ورک، PDO/MySQL، CSS اختصاصی
- **وضعیت:** پروژه دارای سیستم کاربر، رزرو، گالری و مدیریت محتوا است؛ دموهای پورتفولیو نیز از داده‌های پیکربندی‌شده استفاده می‌کنند.
- **طراحی:** Design system مستند در `design-system/majed-mansouri-portfolio/MASTER.md` بر اساس UI/UX Pro Max تولید شده است. برای حفظ هویت بصری قبلی، accent شامپاینی پروژه به‌عنوان override باقی مانده است.

## اقدام‌های انجام‌شده در این مرحله

- صفحه اصلی پورتفولیو با ساختار visual-first، typography بزرگ، فضای تنفس و مسیر روشن به هر دمو بازطراحی شد.
- تصویر معرفی به پرتره استودیویی شفاف‌تر تبدیل شد؛ نسخه مرجع اصلی نیز در assets نگهداری شده است.
- برای هر دمو تصویر Hero اختصاصی و تم رنگی جدا تعریف شد.
- ورود پنل ادمین به CSRF، محدودیت تلاش ورود و session regeneration مجهز شد.
- هدرهای HSTS (برای HTTPS)، Permissions Policy، ضد clickjacking و nosniff تقویت شدند.
- پنل ادمین حالت روشن/تیره پایدار با localStorage دریافت کرد.

## معیارهای کنترل کیفیت پیش از انتشار

1. اجرای `php -l` برای تمام فایل‌های PHP روی محیطی که PHP CLI دارد.
2. بررسی دستی مسیرهای `/`, `/shop`, `/cafe`, `/ticket`, `/company`, `/restaurant`, `/realestate`, `/hotel`, `/medical`, `/admin/login` روی 375، 768، 1024 و 1440 پیکسل.
3. اجرای Lighthouse روی Production و رفع موارد گزارش‌شده؛ نمره ثابت 100 تضمین‌پذیر نیست زیرا به شبکه، فونت و پیکربندی سرور نیز وابسته است.
4. تست فرم‌های رزرو، ورود، آپلود و APIهای ادمین با CSRF نامعتبر، فایل نامعتبر و نشست منقضی‌شده.
5. انتقال secrets از `config/config.php` به متغیرهای محیطی خارج از Git قبل از انتشار عمومی.

## گام‌های بعدی پیشنهادی

- مهاجرت credentials از فایل tracked به فایل local غیرقابل commit.
- افزودن ارسال واقعی ایمیل با provider معتبر و محدودیت IP سمت سرور برای ورود.
- اضافه‌کردن تست‌های integration برای رزرو و APIهای ادمین.
- تفکیک کامل template مشترک دموها به sectionهای اختصاصی برای هر صنعت، پس از تأیید مسیر محتوایی هر کسب‌وکار.
