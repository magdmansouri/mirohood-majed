(()=>{const buttons=[...document.querySelectorAll('[data-filter]')];const targets=[...document.querySelectorAll('[data-sector]')];if(!buttons.length)return;buttons.forEach(button=>{button.setAttribute('aria-pressed',button.classList.contains('active')?'true':'false');button.addEventListener('click',()=>{const filter=button.dataset.filter;buttons.forEach(item=>{item.classList.toggle('active',item===button);item.setAttribute('aria-pressed',item===button?'true':'false')});targets.forEach(item=>item.classList.toggle('is-filtered',filter!=='all'&&item.dataset.sector!==filter));const title=document.querySelector('.mm-section-head h2');if(title)title.setAttribute('aria-live','polite')})})})();
